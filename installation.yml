---

- name: Mise à jour dépots + système
  hosts: localhost
  become: true
  tasks:
    - name: Updating system
      community.general.pacman:
        update_cache: true
        upgrade: true

- name: Installation setup Arch
  hosts: localhost
  # become: true
  roles:
    - role: basic-packages
      tags: basic-packages, server, desktop, wsl
    - role: bat
      tags: bat, server, desktop, wsl
    - role: eza
      tags: eza, server, desktop, wsl
    - role: fastfetch
      tags: fastfetch, server, desktop, wsl
    # - role: find-the-command
    #  tags: find-the-command
    - role: fish
      tags: fish, server, desktop, wsl
    - role: fzf
      tags: fzf, server, desktop, wsl
    - role: terminal
      tags: terminal, desktop
    - role: neovim
      tags: neovim, server, desktop, wsl
    - role: nvchad
      tags: nvchad, server, desktop, wsl
    # - role: onlyoffice
    #  tags: onlyoffice
    - role: starship
      tags: starship, server, desktop, wsl

- name: Install AUR packages
  hosts: localhost
  roles:
    - role: aur
      tags: aur, server, desktop, wsl


- name: Reboot system
  hosts: localhost
  become: true
  tasks:
    - name: Reboot system
      ansible.builtin.pause:
        prompt: "##### Reboot system when the playbook is finished ##### \nPress Enter to continue..."
        seconds: 30
        echo: true
